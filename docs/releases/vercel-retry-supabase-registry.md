# Vercel retry: Supabase FIELD registry

This one-time marker forces Vercel to deploy the complete current `main` state after the earlier rate-limited queue promoted an intermediate commit.

Target release includes:

- Supabase FIELD telemetry and private metrics
- canonical `LD-001 / Bad Day Receipt` identity
- local claim migration
- final analytics and browser regression coverage
